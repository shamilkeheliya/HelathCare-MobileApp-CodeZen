import React  from "react";
//import { useSession } from "../firebase/UserProvider";

const Home = () =>{
    // const {user} = useSession();

    // if(!user){
    //     return null;
    // }

    return(
        <div>
            <p>Home</p>
        </div>
    )
}

export default Home; 